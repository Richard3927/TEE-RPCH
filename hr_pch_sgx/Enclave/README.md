# SGX Enclave (Trusted)

This directory contains the **trusted** TEE implementation for TEE-RPCH:
- enclave entry points (ECALLs) and trusted logic
- `Enclave.edl` interface definition
- enclave configuration (`Enclave.config.xml`)

Build notes and dependencies are documented in `../README.md`.

