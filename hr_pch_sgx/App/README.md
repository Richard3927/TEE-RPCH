# Host App (Untrusted)

This directory contains the **host-side** (untrusted) SGX application code:
- argument parsing / orchestration
- ECALL/OCALL glue (generated files are ignored by git)
- JSON output for experiments

See `../README.md` for build/run instructions.

